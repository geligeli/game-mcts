# Pinned runtime deps for the MCP servers, consumed by bazel's pip.parse
# (hub: @mcp_pip_deps) and by `pip install` when recreating the venv.
# Resolve with: pip install --dry-run --ignore-installed --report report.json \
#     "mcp>=1.0,<2" "protobuf==7.35.1" "grpcio>=1.60" "grpcio-tools>=1.60"
# mcp must stay on 1.x: the servers import mcp.server.fastmcp (removed in 2.0).
# protobuf pin must match the grpcio-tools gencode (7.35.1) — runtime older
# than gencode is rejected at import time. (Bazel flows use @protobuf's own
# 6.32.1 runtime; this pin is for the venv only.)
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
click==8.5.0
cryptography==50.0.1
grpcio==1.83.1
grpcio-tools==1.80.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
httpx-sse==0.4.3
idna==3.19
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
mcp==1.29.1
protobuf==7.35.1
pycparser==3.0
pydantic==2.13.5
pydantic-settings==2.15.0
pydantic_core==2.46.5
PyJWT==2.13.0
python-dotenv==1.2.3
python-multipart==0.0.32
referencing==0.37.0
rpds-py==2026.6.3
setuptools==84.0.0
sse-starlette==3.4.11
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.4
