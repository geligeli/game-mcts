{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Bazel: Build Target (dbg)",
            "type": "bazel",
            "command": "build",
            "options": [
                "-c",
                "dbg"
            ],
            "targets": [
                "//${relativeFileDirname}:${fileBasenameNoExtension}"
            ],
        }
    ]
}